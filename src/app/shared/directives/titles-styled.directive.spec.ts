import { ElementRef, Renderer2 } from '@angular/core';
import { TitlesStyledDirective } from './titles-styled.directive';

describe('TitlesStyledDirective', () => {
  
  it('should create an instance', () => {  
   
    // const directive = new TitlesStyledDirective(element, renderer);
    // expect(directive).toBeTruthy();
  });
});
